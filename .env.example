# Frontend Environment Variables — Copy to .env.local for local development
# For Render Static Site, set these in the Render dashboard Environment variables.

# Set this to your deployed Render backend URL for production.
# Leave UNSET locally — local dev uses Vite proxy to forward /api → localhost:5000
# VITE_API_URL=https://your-backend.onrender.com
